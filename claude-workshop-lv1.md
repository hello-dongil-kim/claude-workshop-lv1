# Claude Workshop Lv.1 — Claude Code를 시작하기 전에

> **대상:** Claude 유료 구독자 (Pro/Max/Teams/Enterprise) + Claude 앱 사용 경험자
> **전제:** Claude Code는 모르는 상태. 터미널 경험 없어도 OK
> **총 소요:** 약 100분 (12파트)
> **사용 방법:** 이 파일을 Claude 앱(데스크톱/웹)에 첨부하고 "실행해줘"

---

## 진행 방법 (튜터 지시)

당신은 이 워크숍의 **튜터**입니다. 아래 규칙을 반드시 따르세요.

### STOP PROTOCOL

각 파트는 반드시 **2턴**에 걸쳐 진행한다.

**Turn 1:**

1. 해당 파트의 EXPLAIN 섹션을 읽고, 사용자에게 친절하게 설명
2. EXECUTE 섹션을 읽고 "직접 해보세요" 안내
3. **반드시 STOP** — 퀴즈 내지 않음

Turn 1 종료 시 필수 문구:

```text
---
위 내용을 직접 해보세요.
끝나면 "완료" 또는 "다음"이라고 입력해주세요.
```

**Turn 2:**

1. 사용자가 "완료/다음" 입력
2. QUIZ 섹션을 읽고 퀴즈 출제
3. 정답/오답 피드백
4. 다음 파트 안내

### 절대 하지 않을 것

- Turn 1에서 퀴즈 내기
- 한 턴에 2개 이상 파트를 동시에 진행
- "해봤나요?" 질문

### 시작

- 사용자가 "실행해줘"라고 하면 아래 파트 목록을 보여주고 시작 파트 선택
- 처음이면 Part 1부터 순서대로 진행 추천

### 톤

- 한국어, 친근하되 전문적
- 기획자·리서처 관점의 예시와 비유 사용
- 전문 용어는 처음 등장 시 반드시 풀어서 설명

---

## 파트 목록

```text
Part 1. 개념 잡기 (~30분)
  1. AI 핵심 용어
  2. Claude 제품 라인업
  3. 컨텍스트 윈도우와 토큰
  4. 보안과 프라이버시

Part 2. 프롬프트 기초 (~20분)
  5. 프롬프트 잘 쓰는 법
  6. 산출물 포맷 전략
  7. 토큰 아끼는 실전 팁

Part 3. 환경 세팅 (~30분)
  8. 터미널/CLI 생존 가이드
  9. Claude Code CLI 설치
  10. IDE 세팅

Part 4. 첫 실습 + 연결 (~20분)
  11. 첫 대화
  12. 다음 단계: Lv.2로 가는 길
```

---

## Part 1: AI 핵심 용어

### EXPLAIN

AI 도구를 제대로 쓰려면 5가지 핵심 용어만 알면 된다.

| 용어 | 뜻 | 기획자용 비유 |
| ---- | -- | ------------- |
| **LLM** | Large Language Model. 대규모 텍스트로 학습한 AI 모델 | 수십억 권의 책을 읽은 신입 사원. 지식은 많지만 우리 팀 규칙은 모름 |
| **토큰** | AI가 텍스트를 처리하는 최소 단위. 한국어 1글자 ≈ 1~2토큰 | 택시 미터기. 입력·출력 모두 토큰 = 비용 |
| **컨텍스트 윈도우** | AI가 한 대화에서 기억할 수 있는 총 용량 | 책상 크기. 넓을수록 많은 자료를 펼쳐놓고 작업 가능 |
| **프롬프트** | AI에게 보내는 지시·질문 텍스트 | 업무 요청서. "잘 해줘"보다 "이 형식으로, 이 관점에서, 3가지로" |
| **할루시네이션** | AI가 사실처럼 보이는 거짓 정보를 생성하는 현상 | 신입이 모르는 걸 아는 척하는 것. 팩트체크 필수 |

**핵심:** 이 5가지를 알면 뒤에 나오는 모든 개념이 자연스럽게 이해된다. 이후 파트에서 모르는 용어가 나오면 **부록 B: 용어 사전**을 참고하자.

### EXECUTE

Claude 앱에서 아래를 직접 입력해보세요:

```text
방금 배운 AI 용어 5가지(LLM, 토큰, 컨텍스트 윈도우, 프롬프트, 할루시네이션)를
내가 팀원에게 설명할 수 있도록, 각각 한 줄로 요약해줘.
기획자가 이해하기 쉬운 비유를 포함해줘.
```

- Claude의 응답을 읽고, 자신만의 표현으로 바꿔볼 수 있는지 확인

### QUIZ

**문제:** "토큰"을 가장 정확하게 설명한 것은?

| 선택지 | 내용 |
| ------ | ---- |
| A | AI가 텍스트를 처리하는 최소 단위이자 비용 계산 기준 |
| B | AI가 학습한 데이터의 양을 나타내는 단위 |
| C | 사용자가 한 번에 입력할 수 있는 글자 수 제한 |

**정답:** A — 토큰은 입력과 출력 모두에 소비되며, 비용의 기본 단위다. 한국어는 1글자당 약 1~2토큰을 소비한다.

---

## Part 2: Claude 제품 라인업

### EXPLAIN

Anthropic의 Claude는 3가지 인터페이스로 제공된다. "assistant → agent" 스펙트럼으로 이해하면 쉽다.

**AI 보조 스펙트럼:**

- **Chat** → 사용자가 작업하고, Claude가 생각·초안을 도움
- **Cowork** → Claude가 작업하고, 사용자가 검토·결정
- **Code** → Claude가 구축하고, 사용자가 설계·지휘

**Claude Code 사용 환경 — 4가지 진입점:**

| 방식 | 설명 | 추천 대상 |
| ---- | ---- | --------- |
| **IDE Extension** (이 워크숍 기준) | VS Code 등에 설치. 파일 변경을 실시간으로 보면서 대화 | 일상적인 작업 전반 |
| **데스크톱 앱** | 독립 실행 앱 (Mac/Windows). Chat/Code/Cowork 3탭 제공 | IDE가 익숙하지 않은 경우 |
| **웹 앱** | claude.ai/code 접속 | 설치 없이 빠르게 시작 |
| **터미널 CLI** | 터미널에서 `claude` 명령으로 실행 | 자동화 연동 |

> **데스크톱 앱의 3탭:** Chat(파일 없이 일반 대화), Code(폴더를 열고 파일 직접 읽기/수정 — 주로 사용), Cowork(클라우드 VM에서 Claude가 백그라운드 독립 작업)

**30초 데모: Code를 쓰면 이렇게 된다**

설치 전이니까 아직 실행은 못 하지만, 이게 뭔지 먼저 보자:

```text
[Chat에서 경쟁사 분석]
사용자: "경쟁사 A, B, C 비교 분석해줘. 한국어, 마크다운 테이블로."
→ 대화창에 텍스트 출력
→ 복사 → 메모장에 붙여넣기 → 파일 저장
→ 다음 세션에서 또: "한국어, 마크다운 테이블로" 반복

[Code에서 같은 작업]
사용자: "경쟁사 A, B, C 비교 분석해줘."
→ competitor-analysis.md 파일이 내 폴더에 바로 생성됨
→ "한국어, 마크다운 테이블" 규칙은 CLAUDE.md에 이미 저장되어 있어서 자동 적용
→ 복사·붙여넣기·반복 설명 = 전부 불필요
```

**이 차이를 만드는 데 코딩은 필요 없다.** CLAUDE.md라는 마크다운 파일 하나만 쓰면 된다.

**"Code는 개발자용 아니야?" — 아니다.**

비교표를 보면 Code의 대상에 "개발자"라고 쓰여 있다. 그래서 "나는 코딩 안 하니까 Chat/Cowork면 충분하지"라고 생각하기 쉽다. 하지만 Code의 진짜 강점은 코딩이 아니다.

| Chat/Cowork의 한계 | Code에서 해결되는 것 |
| ---- | ---- |
| 매 세션마다 같은 규칙을 반복 설명 | CLAUDE.md에 한 번 쓰면 매 세션 자동 적용 |
| "UX 리서치 전문가처럼"을 매번 입력 | Skills로 키워드만 나오면 자동 로딩 |
| Notion/Slack 내용을 수동 복붙 | MCP로 Claude가 직접 읽고 쓰기 |
| 파일 만들면 대화창에 텍스트로 출력 | 내 폴더에 실제 .md, .csv 파일로 생성 |
| 모든 작업을 내가 직접 지시 | Subagent에게 위임하고 결과만 받기 |
| 나만 쓸 수 있는 프롬프트 노하우 | Plugin으로 팀 전체에 배포 |

**핵심:** Code는 "코딩 도구"가 아니라 **"나만의 AI 시스템을 설계하는 도구"**다. 여러분이 이미 잘 하는 "문서로 설계하기"가 곧 Code의 확장 역량이 된다. CLAUDE.md, Skills 파일, Hook 설정 — 전부 마크다운과 JSON 파일이다. 코드를 한 줄도 쓸 필요 없다. (Lv.2에서 직접 만들어본다.)

**비개발자가 Code를 쓰면 달라지는 것:**

| 상황 | Chat/Cowork | Code |
| ---- | ----------- | ---- |
| "경쟁사 분석해줘" | 대화창에 텍스트 출력 → 복사 → 파일 저장 | `competitor-analysis.md` 파일이 내 폴더에 바로 생성 |
| "매주 월요일 주간 보고서" | 매주 같은 프롬프트 입력 | CLAUDE.md + Skill로 자동 포맷 적용 |
| "Notion에서 리서치 노트 가져와" | Notion 열기 → 복사 → Claude에 붙여넣기 | MCP로 Claude가 Notion에서 직접 읽기 |
| "이 분석을 팀원도 같은 방식으로" | 프롬프트를 메신저로 공유 → 각자 복붙 | Plugin으로 한 번에 설치 |

**실제 시간 절약 시나리오:**

| 작업 | Chat/Cowork 소요 | Code 소요 | 절약 | 왜? |
| ---- | ---------------- | --------- | ---- | --- |
| 경쟁사 분석 보고서 | 30분 (프롬프트 작성 + 결과 복사 + 파일 저장 + 포맷 정리) | 10분 (요청 한 줄 → 파일 자동 생성, 포맷은 CLAUDE.md가 처리) | 20분 | 복붙·포맷·저장 제거 |
| 주간 보고서 (매주 반복) | 매주 15분 (같은 프롬프트 + 규칙 반복 입력) | 매주 5분 (Skill 자동 로딩 + 포맷 자동) | 매주 10분 | 반복 설명 제거 |
| 리서치 노트 정리 (Notion 기반) | 25분 (Notion→복사→붙여넣기→정리 요청) | 8분 (MCP로 Notion 직접 접근 → 정리 → 파일 생성) | 17분 | 복붙 제거 |
| 팀원 5명에게 워크플로우 공유 | 1시간+ (프롬프트 공유 → 각자 세팅 → 결과 불일치 → 재조정) | 10분 (Plugin ZIP 배포 → 전원 동일 환경) | 50분+ | 수동 세팅 제거 |

> **핵심 계산:** 주 3회 반복 작업 기준, Code 전환 시 **월 4~8시간 절약**. 초기 학습(Lv.1+Lv.2)에 4시간 투자하면, **첫 달부터 투자 회수**.

**상세 비교표:**

| 항목 | Chat | Cowork | Code |
| ---- | ---- | ------ | ---- |
| **뭐하는 것** | 대화형 AI 어시스턴트 | 데스크톱 AI 협업 도구 | 터미널/IDE/웹/데스크톱 에이전트 |
| **어디서 실행** | 웹, 모바일, 데스크톱 앱 | 데스크톱 앱 + 모바일 앱 (macOS/Windows/iOS/Android) | 터미널/IDE/웹/데스크톱 |
| **파일 접근** | 수동 업로드만 | 로컬 폴더 직접 읽기/쓰기 | 전체 파일시스템 접근 |
| **작업 방식** | 1 프롬프트 → 1 응답 | 목표 입력 → 완성된 산출물 | 목표 입력 → 완성된 프로젝트 |
| **기술 수준** | 없어도 됨 | 없어도 됨 | 터미널 기초 필요 |
| **메모리** | 세션 내에서만 | 지속적 (대화 기록 유지) | Memory + CLAUDE.md |
| **컴퓨터 제어** | 불가 | 가능 (Pro/Max 베타) | 가능 |
| **예약 작업** | 불가 | 가능 (반복/예약) | 가능 |
| **대상** | 모든 사람 | 비개발 지식 노동자 | 개발자 + **비개발자도 확장 가능** |

**Code의 권한 모드 — 안전하게 시작하기:**

| 모드 | 동작 | 추천 상황 |
| ---- | ---- | --------- |
| **Plan mode** | 읽기만 가능, 변경 불가 | 탐색·분석, 전략 수립 |
| **Ask permissions** (기본값) | 매 작업마다 허락 요청 | **초보자에게 추천** |
| **Auto accept edits** | 파일 수정은 자동, 명령 실행은 허락 | 익숙해진 후 |
| **Bypass permissions** | 모든 작업 자동 승인 | 사용 비권장 (되돌리기 어려움) |

> **초보자 추천:** Ask permissions로 시작 → 익숙해지면 Auto accept edits로 전환. Bypass permissions는 사용을 피할 것. 권한 모드를 활용한 4단계 작업법은 **Part 11**에서 직접 체험한다.

**언제 뭘 쓸까?**

| 상황 | 추천 | 예시 프롬프트 |
| ---- | ---- | ------------- |
| 빠른 질문/초안 | Chat | "Q3 예산 삭감 공지 이메일 초안 써줘" |
| 문서 리뷰/의견 | Chat | "이 보고서 초안에서 빠진 게 뭐야?" |
| 회의 준비 | Chat | "내일 이사회에서 나올 질문 5개와 답변 준비해줘" |
| 파일 읽고 보고서 작성 | Cowork | "이 3개 Excel 파일 읽고 부서별 차이 분석 보고서 써줘" |
| 파일 정리/이름 변경 | Cowork | "이 폴더의 파일을 YYYY-MM-DD_유형 형식으로 정리해줘" |
| 데이터 추출 | Cowork | "이 PDF 50개에서 업체명, 금액, 날짜 추출해서 Excel로" |
| 프로젝트 규칙 자동 적용 | Code | "CLAUDE.md에 팀 규칙 저장 → 매 세션 자동 로딩" |
| 전문 지식 자동 트리거 | Code | "UX 리서치 Skill 만들기 → '인터뷰' 키워드에 자동 활성화" |
| 외부 도구 직접 연결 | Code | "MCP로 Notion 연결 → 리서치 노트 자동 가져오기" |
| 워크플로우 팀 배포 | Code | "내 Plugin을 팀 전원에게 한 번에 설치" |

> **세 모드 모두의 한계:** 실시간 데이터 접근 불가, 100% 정확한 계산 보장 안 됨, 전문적 판단 대체 불가, 민감 데이터 주의. 자세한 내용은 Part 4에서 다룬다.

### EXECUTE

현재 Claude를 어떻게 쓰고 있는지 돌아보세요:

```text
나는 기획자(또는 리서처/PM)인데, 최근 Claude를 써서 했던 작업 3가지를 알려줄게.
각각 Chat, Cowork, Code 중 어떤 모드가 가장 적합한지 판단해줘.
이유와 함께, 해당 모드에서 쓸 수 있는 예시 프롬프트도 제안해줘.
```

- 내가 쓰던 작업 3가지를 직접 입력하고, Claude의 판단을 확인

### QUIZ

**문제:** Claude Code가 Chat/Cowork와 근본적으로 다른 점은?

| 선택지 | 내용 |
| ------ | ---- |
| A | 더 긴 대화를 할 수 있다 |
| B | 내 컴퓨터의 파일을 직접 읽고, 만들고, 수정할 수 있다 |
| C | 더 최신 정보를 알고 있다 |

**정답:** B — Chat은 대화창에 텍스트를 출력하고, Cowork는 데스크톱 앱에서 작업하지만, Code는 사용자의 로컬 파일시스템 전체에 직접 접근하여 파일을 만들고 수정한다. 이것이 "내 컴퓨터에 상주하는 전담 팀원" 비유의 핵심이다.

---

## Part 3: 컨텍스트 윈도우와 토큰

### EXPLAIN

컨텍스트 윈도우는 AI가 한 대화에서 "기억"할 수 있는 총 용량이다.

**책상 비유:**

```text
작은 책상 (컨텍스트 윈도우가 작은 모델)
→ 자료 3장만 펼쳐놓을 수 있음
→ 새 자료를 올리면 오래된 자료가 바닥에 떨어짐

넓은 책상 (컨텍스트 윈도우가 큰 모델)
→ 자료 100장을 펼쳐놓고 참조 가능
→ 하지만 너무 많으면 어디에 뭐가 있는지 헷갈림
```

**Claude 모델별 컨텍스트 윈도우:**

| 모델 | 컨텍스트 윈도우 | 비유 |
| ---- | --------------- | ---- |
| Claude Haiku | 200K 토큰 | 일반 사무실 책상 |
| Claude Sonnet | 200K 토큰 | 넓은 회의 테이블 |
| Claude Opus | 200K~1M 토큰 (플랜에 따라 다름) | 대형 작전실 |

> **참고:** 컨텍스트 윈도우 크기는 모델과 구독 플랜에 따라 달라질 수 있다. 위 수치는 대략적인 기준이다.

**토큰 비용 구조:**

- **입력 토큰**: 내가 보내는 텍스트 (프롬프트 + 첨부 파일)
- **출력 토큰**: Claude가 응답하는 텍스트
- 입력보다 **출력이 더 비쌈** (보통 3~5배)
- 대화가 길어질수록 매 턴마다 **이전 대화 전체**를 다시 입력하는 구조 → 비용 누적

**핵심:** 대화가 길어지면 ① 품질이 떨어지고 ② 비용이 늘어난다. 이것이 "새 세션 전략"과 "토큰 절약"이 중요한 이유다.

**컨텍스트 관리 명령어 (Claude Code 전용):**

| 명령어 | 용도 | 언제 사용하나 |
| ------ | ---- | ------------- |
| `/clear` | 대화 내용 완전 초기화 | 새로운 작업 시작 시 (**가장 중요한 습관**) |
| `/compact` | 대화를 요약하여 압축 | 대화가 길어졌지만 맥락을 유지하고 싶을 때 |
| `/context` | 컨텍스트 사용량 시각화 | 얼마나 찼는지 확인 |
| `/cost` | 현재 사용량·비용 확인 | 비용 모니터링 |
| `/recap` | 이전 세션 요약 | 작업을 이어서 할 때 |

**실전 관리 전략:**

- **① 작업 간 `/clear` 습관화** — 관련 없는 작업 사이에 반드시 `/clear`. 가장 효과적인 컨텍스트 관리법
- **② 수정이 2회 실패하면 리셋** — 같은 대화에서 계속 고치려 하지 말고, `/clear` 후 더 명확한 지시로 재시작
- **③ `/compact`로 압축** — `/clear`처럼 다 날리기는 아깝고, 맥락은 유지하고 싶을 때 사용

**자동 메모리 시스템:**

Claude Code는 대화에서 배운 내용(사용자 선호, 중요한 발견 등)을 자동으로 기억한다. 매 세션 시작 시 자동 로드되어, 같은 설명을 반복할 필요가 없다. `/memory`로 저장된 내용을 확인·편집할 수 있다.

> **CLAUDE.md와의 차이:** 메모리는 Claude Code가 **자동으로 관리**하는 시스템(대화 학습 누적). CLAUDE.md는 **사용자가 직접 작성**하는 프로젝트 규칙 파일. 두 시스템은 별개로 작동하며, CLAUDE.md는 **Lv.2 Session 1**에서 다룬다.

### EXECUTE

Claude에서 직접 확인해보세요:

```text
이 대화의 현재 토큰 사용량이 대략 얼마인지 알려줘.
그리고 내가 10번 더 대화하면 토큰이 어떻게 누적되는지 설명해줘.
```

- 토큰이 대화마다 누적되는 구조를 체감

### QUIZ

**문제:** 대화가 길어질수록 토큰 비용이 급격히 늘어나는 이유는?

| 선택지 | 내용 |
| ------ | ---- |
| A | Claude의 응답이 점점 길어지기 때문 |
| B | 매 턴마다 이전 대화 전체를 다시 입력하는 구조이기 때문 |
| C | 오래된 대화일수록 처리 비용이 높아지기 때문 |

**정답:** B — Claude는 매 턴마다 대화 히스토리 전체를 컨텍스트로 다시 읽는다. 10번째 턴에서는 1~9번째 대화 전체 + 새 입력이 모두 토큰으로 소비된다.

---

## Part 4: 보안과 프라이버시

### EXPLAIN

"내 데이터가 AI 학습에 쓰이나?" — 기획자가 가장 먼저 묻는 질문이다.

**핵심 원칙:**

| 질문 | 답변 |
| ---- | ---- |
| 내 대화가 모델 학습에 쓰이나? | **아니요.** Anthropic은 API/유료 플랜 대화를 모델 학습에 사용하지 않음 |
| 내 데이터가 서버에 저장되나? | 대화 기록은 일정 기간 저장 (계정별 히스토리). 삭제 가능 |
| 다른 사용자가 내 대화를 볼 수 있나? | **아니요.** 대화는 계정에 귀속 |
| Claude Code에서 내 파일이 전송되나? | 작업에 필요한 파일 내용이 API로 전송됨. 학습에는 미사용 |

**플랜별 차이:**

| 플랜 | 데이터 보호 수준 |
| ---- | ---------------- |
| Pro/Max | 대화 학습 미사용. 30일 보관 후 삭제 가능 |
| Teams | Pro + 팀 관리자 제어. 데이터 격리 |
| Enterprise | 전용 인스턴스, SSO, 감사 로그, 커스텀 보관 정책 |

**Chat/Cowork/Code 공통 한계 — 반드시 기억할 것:**

| 한계 | 설명 |
| ---- | ---- |
| 실시간 데이터 불가 | 주가, 오늘 뉴스 등은 접근 불가 (웹 검색 도구 연결 시 가능) |
| 100% 정확한 계산 보장 안 됨 | 재무 수치, 통계는 반드시 사람이 검증 |
| 전문적 판단 대체 불가 | 컴플라이언스, 세무, 법률 사안은 전문가 확인 필수 |
| 민감 데이터 주의 | Enterprise가 아닌 환경에서는 민감 정보 입력 최소화 |

**기획자를 위한 실전 가이드:**

- 민감 데이터(개인정보, 계약서, 재무): 꼭 필요한 부분만 익명화하여 전달
- 사내 기밀: Teams/Enterprise 플랜 권장
- 팩트체크: Claude의 답변은 반드시 원본 자료와 교차 검증
- 할루시네이션 대응: "모르면 모른다고 해"를 프롬프트에 명시

### EXECUTE

Claude에서 직접 확인해보세요:

```text
내가 지금 쓰고 있는 Claude 플랜이 뭔지 알 수 있어?
그리고 이 대화 내용이 AI 학습에 사용되는지 알려줘.
```

- Claude의 답변을 확인하고, 자신의 플랜에 맞는 데이터 보호 수준을 파악

### QUIZ

**문제:** Claude 유료 플랜(Pro/Max/Teams/Enterprise)의 데이터 정책으로 맞는 것은?

| 선택지 | 내용 |
| ------ | ---- |
| A | 유료 플랜도 대화 내용이 모델 학습에 일부 사용된다 |
| B | 유료 플랜은 대화 내용을 저장하지 않는다 |
| C | 유료 플랜은 대화를 모델 학습에 사용하지 않으며, 대화 기록 삭제가 가능하다 |

**정답:** C — Anthropic은 유료 플랜 대화를 모델 학습에 사용하지 않는다. 대화 기록은 계정에 보관되지만 사용자가 삭제할 수 있다.

---

## Part 5: 프롬프트 잘 쓰는 법

### EXPLAIN

프롬프트는 "질문"이 아니라 **"업무 요청서"**다. 같은 질문이라도 어떻게 쓰느냐에 따라 결과 품질이 완전히 달라진다.

**Bad → Good → Great 비교:**

| 단계 | 경쟁사 분석 예시 |
| ---- | --------------- |
| **Bad** | "경쟁사 분석해줘" |
| **Good** | "이 경쟁사 3곳의 강점과 약점을 분석해줘" |
| **Great** | 아래 참조 |

Great 프롬프트 예시:

```text
너는 시장 분석 전문가야.
회사: [우리 회사명]. 경쟁사: [A사], [B사].
포지셔닝, 가격 전략, 마케팅 채널을 비교 분석해줘.
가장 큰 강점과 약점을 각각 식별해줘.
표면적 관찰이 아닌 실질적 인사이트를 줘.
시장에서 우리의 기회를 찾아줘.
분석을 단계별로 작성해줘.
결과는 비교 요약 테이블로.
```

**왜 Great인가?** 아래 8가지 구성 요소가 모두 들어가 있기 때문이다.

**프롬프트의 8가지 재료:**

| # | 요소 | 설명 | 위 예시에서 |
| - | ---- | ---- | ----------- |
| 1 | **Task context** | 역할/상황 설정 | "시장 분석 전문가" |
| 2 | **Tone** | 톤 지정 | "실질적 인사이트" (전략적·분석적) |
| 3 | **Background** | 배경 정보 | 회사명, 경쟁사 목록 |
| 4 | **Task rules** | 구체적 제약 조건 | "표면적 관찰이 아닌" |
| 5 | **Examples** | 예시 제공 | (이 예시에는 없음 — 선택사항) |
| 6 | **History** | 경험/맥락 기반 | "시장에서 기회를 찾아줘" |
| 7 | **Request** | 요청 + 사고 방식 | "단계별로 작성해줘" |
| 8 | **Output** | 산출물 형식 | "비교 요약 테이블" |

8가지를 전부 넣을 필요는 없다. **상황에 맞는 재료를 골라서** 넣으면 된다.

**실전 공식: ODAF (기획자 추천)**

8가지를 매번 기억하기 어렵다면, **ODAF 4가지**만 기억하면 된다:

| 요소 | 뜻 | 질문 | 예시 |
| ---- | -- | ---- | ---- |
| **O** — Outcome | 원하는 결과물 | "뭘 받고 싶은가?" | "부서별 차이 분석 Word 문서" |
| **D** — Data | 입력 데이터 | "뭘 줄 건가?" | "이 폴더의 Excel 3개 파일" |
| **A** — Action | 수행할 작업 | "뭘 해야 하나?" | "실적 vs 예산 비교, 10% 이상 차이 강조" |
| **F** — Format | 산출물 형식 | "어떤 형태로?" | "마크다운, 한국어, 테이블 포함" |

ODAF 예시 — 경쟁사 분석:

```text
O: 경쟁사 3곳 비교 분석 보고서
D: 경쟁사 A, B, C의 공식 웹사이트 정보
A: 포지셔닝, 가격, 기능을 비교하고 우리의 차별점 도출
F: 마크다운 테이블 + 핵심 인사이트 3줄 요약
```

**8요소 vs ODAF — 언제 뭘 쓸까?**

| 상황 | 추천 |
| ---- | ---- |
| 간단한 요청 | ODAF 4요소면 충분 |
| 복잡하거나 톤/맥락이 중요한 작업 | 8요소에서 필요한 것을 추가 |

### EXECUTE

ODAF 공식으로 자신의 업무에 맞는 프롬프트를 만들어보세요:

```text
아래 ODAF 공식을 채워서 프롬프트를 만들어줘.
내 업무: [여기에 자신의 실제 업무 하나를 입력]

O (Outcome, 결과물): ?
D (Data, 입력 데이터): ?
A (Action, 수행 작업): ?
F (Format, 산출물 형식): ?

완성된 프롬프트를 Bad/Good/Great 3단계로 보여주고,
각 단계에서 뭐가 추가되었는지 설명해줘.
```

- 완성된 Great 프롬프트를 실제로 실행해보고 결과 확인

### QUIZ

**문제:** ODAF 공식에서 "O"가 의미하는 것은?

| 선택지 | 내용 |
| ------ | ---- |
| A | Output — 산출물 형식 (마크다운, CSV 등) |
| B | Operation — 수행할 작업 (분석, 비교 등) |
| C | Outcome — 원하는 최종 결과물 |

**정답:** C — O는 Outcome(원하는 결과물). "뭘 받고 싶은가?"에 대한 답이다. Output(산출물 형식)은 F(Format)에 해당한다. Outcome을 먼저 정의해야 나머지 D, A, F를 구체화할 수 있다.

---

## Part 6: 산출물 포맷 전략

### EXPLAIN

AI 산출물은 **마크다운(.md)** 또는 **CSV(.csv)** 로 받는 것이 좋다.

**왜 md/csv인가?**

| 포맷 | 장점 | 적합한 상황 |
| ---- | ---- | ----------- |
| **마크다운 (.md)** | 어디서든 열림, 구조적, 버전 관리 용이, 가벼움 | 보고서, PRD, 회의록, 리서치 노트 |
| **CSV (.csv)** | 어디서든 열림, 데이터 정제 용이, 엑셀 호환 | 데이터 분석, 목록, 비교표 |
| ~~Word (.docx)~~ | 호환성 문제, 무거움, 버전 관리 어려움 | (비추천) |
| ~~PDF~~ | 수정 불가, 재활용 어려움 | (최종 배포용으로만) |

**마크다운 기초 문법 — 5가지만 알면 된다:**

```markdown
# 제목 1 (가장 큰 제목)
## 제목 2
### 제목 3

**굵게** — 강조할 때
*기울임* — 부가 설명

- 목록 항목 1
- 목록 항목 2
  - 하위 항목

| 열1 | 열2 | 열3 |
| --- | --- | --- |
| 값1 | 값2 | 값3 |

> 인용문 — 참고 사항이나 주의 표시
```

**기획자용 실전 팁:** Claude에게 "마크다운으로 작성해줘"라고 지정하면, 제목·목록·테이블이 자동으로 구조화된 문서를 받을 수 있다. 더 많은 문법은 **부록 A: 마크다운 치트시트**에 정리되어 있다.

### EXECUTE

Claude에서 직접 해보세요:

```text
"사용자 인터뷰 5건의 핵심 인사이트"를 정리한다고 가정하고,
마크다운 형식으로 예시 문서를 만들어줘.
제목, 테이블, 목록, 인용문을 모두 포함해줘.
```

- 결과를 복사해서 텍스트 편집기에 붙여넣고, `.md` 파일로 저장해보세요
- Obsidian이나 VS Code에서 열어보면 예쁘게 렌더링되는 것을 확인

### QUIZ

**문제:** AI 산출물을 마크다운(.md)으로 받는 가장 큰 이유는?

| 선택지 | 내용 |
| ------ | ---- |
| A | 구조적이고, 어디서든 열리며, 버전 관리와 재활용이 쉬워서 |
| B | 파일 크기가 작아서 |
| C | Claude가 마크다운만 지원해서 |

**정답:** A — 마크다운은 플레인 텍스트이므로 어떤 환경에서든 열리고, 제목·목록·테이블 등 구조를 표현할 수 있으며, Git 등으로 버전 관리가 쉽다. Claude는 다른 포맷도 지원하지만, 재활용성 관점에서 md가 최적이다.

---

## Part 7: 토큰 아끼는 실전 팁

### EXPLAIN

토큰은 비용이다. 같은 품질의 결과를 더 적은 토큰으로 얻는 7가지 팁. Part 3에서 배운 `/clear`·`/compact`가 여기서 실전으로 연결된다.

| # | 팁 | 효과 | 예시 |
| - | -- | ---- | ---- |
| 1 | **새 세션 전략** (`/clear`) | 토큰 누적 방지 | 주제 바뀌면 `/clear`로 초기화 |
| 2 | **불필요한 반복 제거** | 입력 토큰 절약 | "앞에서 말한 것처럼" 대신 핵심만 재지시 |
| 3 | **출력 길이 지정** | 출력 토큰 절약 | "3줄로 요약해줘" / "테이블로 정리해줘" |
| 4 | **모델 선택** | 비용 최적화 | 간단한 작업은 Haiku, 복잡한 판단은 Opus |
| 5 | **파일 첨부 최소화** | 입력 토큰 절약 | 100페이지 PDF 전체 대신, 관련 부분만 발췌 |
| 6 | **단계 분리** | 중간 결과 검증 | "먼저 목차만" → 확인 → "3번 섹션 작성해줘" |
| 7 | **컨텍스트 압축** (`/compact`) | 장기 대화 효율화 | `/compact`로 대화 요약 압축. 맥락 유지하며 토큰 절약 |

**가장 효과적인 팁:** ① 새 세션 전략(`/clear`) + ③ 출력 길이 지정. 이 두 가지만으로 토큰 소비를 50% 이상 줄일 수 있다.

### EXECUTE

실제로 토큰 절약 기법을 체험해보세요:

```text
"경쟁사 분석 보고서"를 작성한다고 가정하자.
아래 두 프롬프트의 차이를 비교하고, 어느 쪽이 토큰을 절약하는지 설명해줘.

프롬프트 A:
"경쟁사 분석해줘. 자세하게."

프롬프트 B:
"경쟁사 3곳(A, B, C)을 비교하는 테이블을 만들어줘.
열: 회사명, 주요 기능, 가격, 강점, 약점.
5열 x 3행으로 작성."
```

### QUIZ

**문제:** 토큰 비용을 줄이는 가장 효과적인 습관은?

| 선택지 | 내용 |
| ------ | ---- |
| A | 가능한 한 짧은 단어만 사용한다 |
| B | 주제가 바뀌면 새 세션을 시작하고, 출력 길이를 구체적으로 지정한다 |
| C | 항상 가장 저렴한 모델만 사용한다 |

**정답:** B — 새 세션은 토큰 누적을 방지하고, 출력 길이 지정은 불필요한 출력을 줄인다. 짧은 단어 사용은 효과가 미미하고, 저렴한 모델만 쓰면 품질이 떨어진다.

---

## Part 8: 터미널/CLI 생존 가이드

### EXPLAIN

Claude Code를 설치하려면 **터미널**을 사용해야 한다. 솔직하게 말하면:

**해야 할 것:**
- 터미널 명령어 5개 이해하기 (Part 8 — 지금)
- 설치 명령어 1개 복사·붙여넣기 (Part 9)
- IDE에서 Extension 설치 + 폴더 열기 (Part 10)

총 3단계, 약 30분 소요. 코딩은 없다. 명령어를 외울 필요도 없다 — 이 문서에서 복사해서 붙여넣기만 하면 된다. 그리고 막히면? Claude에게 "안 돼, 도와줘"라고 말하면 된다 (Part 9 설치 후부터).

**터미널이란?**
텍스트로 컴퓨터에 명령을 내리는 도구. 마우스 대신 키보드로 "이 폴더 열어줘", "이 프로그램 설치해줘"를 입력하는 방식이다.

**터미널 여는 법:**

| OS | 방법 |
| -- | ---- |
| **macOS** | `Cmd + Space` → "터미널" 검색 → Enter. 또는 Finder → 응용 프로그램 → 유틸리티 → 터미널 |
| **Windows** | `Win + X` → "Windows PowerShell" 또는 "터미널" 클릭. 또는 `Win + R` → `powershell` → Enter |

**5가지 생존 명령어:**

| 명령어 | 뜻 | 예시 | 기획자용 비유 |
| ------ | -- | ---- | ------------- |
| `pwd` | 현재 위치 확인 | `pwd` → `/Users/이름` | "나 지금 어디 있지?" |
| `ls` | 현재 위치의 파일 목록 | `ls` → 파일들 나열 | "여기에 뭐가 있지?" |
| `cd 폴더명` | 폴더 이동 | `cd Desktop` | "이 폴더로 이동" |
| `cd ..` | 상위 폴더로 이동 | `cd ..` | "뒤로 가기" |
| `mkdir 폴더명` | 새 폴더 만들기 | `mkdir my-project` | "새 작업 공간 만들기" |

> **Windows 참고:** PowerShell에서도 `pwd`, `ls`, `cd`, `mkdir`이 모두 동일하게 작동한다. 별도로 외울 것 없다.

**복사·붙여넣기:**

| 동작 | macOS | Windows |
| ---- | ----- | ------- |
| 복사 | `Cmd + C` | `Ctrl + C` |
| 붙여넣기 | `Cmd + V` | `Ctrl + V` (또는 마우스 우클릭) |

명령어를 외울 필요 없다. 이 문서에서 복사해서 붙여넣기만 하면 된다.

### EXECUTE

터미널(macOS) 또는 PowerShell(Windows)을 열고 아래를 순서대로 입력해보세요.
실행 결과를 이 대화에 붙여넣으면 튜터가 확인해줍니다:

**Step 1:** 현재 위치 확인

```bash
pwd
```

**Step 2:** 현재 위치의 파일 목록 보기

```bash
ls
```

**Step 3:** 바탕화면(Desktop)으로 이동

```bash
cd Desktop
```

**Step 4:** 새 폴더 만들기

```bash
mkdir my-first-project
```

**Step 5:** 다시 상위 폴더로 돌아오기

```bash
cd ..
```

- 5개 명령어를 순서대로 실행하고, 각 결과를 확인
- macOS와 Windows 모두 동일한 명령어가 작동한다

### QUIZ

**문제:** 바탕화면에 있는 `project` 폴더로 이동하려면 어떤 순서가 맞는가?

| 선택지 | 내용 |
| ------ | ---- |
| A | `ls Desktop/project` |
| B | `pwd project` |
| C | `cd Desktop` → `cd project` |

**정답:** C — `cd`는 폴더 이동 명령어. 먼저 Desktop으로 이동하고, 그 안의 project 폴더로 다시 이동한다. `ls`는 목록 보기, `pwd`는 현재 위치 확인이지 이동 명령이 아니다.

---

## Part 9: Claude Code CLI 설치

### EXPLAIN

Claude Code는 터미널에서 한 줄 명령어로 설치할 수 있다.

**시스템 요구사항:**

| 항목 | macOS | Windows |
| ---- | ----- | ------- |
| OS | macOS 13.0 (Ventura) 이상 | Windows 10 (1809) 이상 또는 Windows 11 |
| RAM | 4GB 이상 | 4GB 이상 |
| Shell | Bash 또는 Zsh (기본: Zsh) | PowerShell 5.1 이상 (기본 내장) |
| 인터넷 | 필수 | 필수 |
| Claude 구독 | Pro, Max, Teams, Enterprise 중 하나 | 동일 |

**설치 방법 비교:**

| 방법 | OS | 명령어 | Node.js | 자동 업데이트 |
| ---- | -- | ------ | ------- | ------------- |
| **네이티브 (권장)** | macOS | `curl -fsSL https://claude.ai/install.sh \| bash` | 불필요 | 자동 |
| **PowerShell (권장)** | Windows | `irm https://claude.ai/install.ps1 \| iex` | 불필요 | 자동 |
| 설치 파일 | Windows | `https://claude.ai/download` | 불필요 | 자동 |
| Homebrew | macOS | `brew install --cask claude-code` | 불필요 | 수동 |
| ~~npm (Deprecated)~~ | 전체 | ~~`npm install -g @anthropic-ai/claude-code`~~ | ~~필요~~ | ~~수동~~ |

> **주의:** 블로그나 구글 검색에서 `npm install` 방식을 안내하는 글이 많지만, 이 방식은 더 이상 공식 권장되지 않는다.

**설치 명령어 (공식):**

macOS:

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

Windows (PowerShell):

```powershell
irm https://claude.ai/install.ps1 | iex
```

한 줄이 전부다. 복사해서 붙여넣고 Enter.

> **Windows:** 보안 경고가 뜨면 "실행" 또는 "허용"을 클릭. 위 방법이 안 될 경우 `https://claude.ai/download`에서 설치 파일(.exe)을 직접 다운로드.

**설치 확인:**

```bash
claude --version
```

버전 번호가 출력되면 설치 성공. `command not found` 또는 `not recognized` → 터미널/PowerShell을 닫고 새로 열기.

**최초 로그인:**

```bash
claude
```

처음 실행하면 인증 과정이 나온다. 브라우저가 열리면 Claude 계정으로 로그인하면 된다.

**자주 발생하는 문제:**

| 증상 | OS | 해결 |
| ---- | -- | ---- |
| `command not found: claude` | macOS | 터미널을 닫고 새로 열기. 또는 `source ~/.zshrc` 실행 |
| `'claude' is not recognized` | Windows | PowerShell을 닫고 새로 열기 |
| 설치 중 권한 오류 | macOS | `sudo` 붙이기: `curl -fsSL https://claude.ai/install.sh \| sudo bash` |
| `실행 정책` 오류 | Windows | `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` 실행 후 재시도 |
| 로그인 브라우저가 안 열림 | 공통 | 터미널에 표시된 URL을 직접 복사해서 브라우저에 붙여넣기 |

### EXECUTE

아래를 **순서대로** 실행해보세요.
각 단계의 결과를 이 대화에 붙여넣으면 튜터가 성공 여부를 확인해줍니다:

**Step 1:** 설치

macOS:

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

Windows (PowerShell):

```powershell
irm https://claude.ai/install.ps1 | iex
```

- 설치가 완료되면 완료 메시지 확인

**Step 2:** 터미널/PowerShell을 닫고 새로 열기 → 설치 확인

```bash
claude --version
```

- 버전 번호(예: `2.x.x`)가 표시되면 성공

**Step 3:** 최초 로그인

```bash
claude
```

- 브라우저에서 인증 완료 후 터미널로 돌아오기
- "Welcome to Claude Code!" 메시지가 보이면 성공
- `exit` 또는 `/exit`으로 종료

### QUIZ

**문제:** Claude Code를 설치했는데 블로그에서 `npm install -g @anthropic-ai/claude-code` 방식을 발견했다. 어떻게 해야 하나?

| 선택지 | 내용 |
| ------ | ---- |
| A | npm 방식이 더 최신이니 npm으로 재설치한다 |
| B | 이미 curl로 설치했으면 그대로 사용한다. npm은 더 이상 권장되지 않는다 |
| C | 두 방식을 모두 설치해야 완전한 기능을 쓸 수 있다 |

**정답:** B — npm 방식은 Deprecated(지원 중단). curl 네이티브 설치가 공식 권장 방식이며, Node.js 없이도 설치되고 자동 업데이트도 지원한다. 블로그의 옛 정보에 속지 말 것.

---

## Part 10: IDE 세팅

### EXPLAIN

Claude Code는 **4가지 환경**에서 사용할 수 있다:

| 환경 | 설명 | 추천 상황 |
| ---- | ---- | --------- |
| **IDE Extension** (1순위 추천) | VS Code 등에 설치. 파일 변경을 실시간으로 보면서 대화 | 일상적인 작업 전반 |
| **데스크톱 앱** | 독립 실행 앱 (Mac/Windows). IDE 없이도 사용 가능 | IDE가 익숙하지 않은 경우 |
| **웹 앱** | claude.ai/code에서 브라우저로 접속 | 설치 없이 빠르게 시작할 때 |
| **터미널 CLI** | 터미널에서 `claude` 명령으로 실행 | 가볍게 쓸 때, 자동화 연동 |

이 워크숍에서는 **IDE Extension**을 기준으로 진행한다. 파일 생성·수정을 에디터에서 바로 확인할 수 있어 가장 편리하기 때문이다.

**왜 IDE가 편한가?**

| 이유 | 설명 |
| ---- | ---- |
| 실시간 파일 반영 | Claude가 만든 파일을 에디터에서 즉시 확인 |
| 폴더 기반 작업 | 프로젝트 폴더를 열면 Claude Code가 해당 폴더 기준으로 작업 |
| 무료 + 크로스 플랫폼 | macOS, Windows 모두 동일한 경험 |

**추천 IDE (VS Code 기반이면 모두 가능):**

| IDE | 특징 |
| --- | ---- |
| **VS Code** (1순위 추천) | 무료, 가장 넓은 생태계, Claude Code 공식 지원 |
| Cursor | AI 코딩 특화, VS Code 기반 |
| Google Antigravity | AI 친화적, VS Code 기반 |

**IDE Extension 설치 순서:**

1. IDE 설치 (아직 없다면 [VS Code 다운로드](https://code.visualstudio.com) 권장)
2. 실행 → Extensions (좌측 사이드바 블록 아이콘 또는 macOS `Cmd+Shift+X` / Windows `Ctrl+Shift+X`)
3. "Claude Code" 검색 → **Anthropic**이 게시자인 항목 → **Install**
4. 설치 완료 → 좌측 사이드바에 **Claude 아이콘** 표시

> **IDE 없이 시작하고 싶다면:** 데스크톱 앱(`https://claude.ai/download`)을 설치하거나, 웹 앱(`claude.ai/code`)에 접속해도 된다. 기능은 동일하다.

### EXECUTE

아래를 순서대로 해보세요.
막히는 부분이 있으면 상황을 이 대화에 설명해주세요:

**Step 1:** IDE 실행 (설치가 안 됐다면 `https://code.visualstudio.com`에서 VS Code 다운로드)

**Step 2:** Extensions에서 "Claude Code" 검색 → Anthropic이 게시자인 항목 Install
- macOS: `Cmd+Shift+X` / Windows: `Ctrl+Shift+X`

**Step 3:** 아무 폴더를 열기 (File → Open Folder → 바탕화면이든 어디든)

**Step 4:** Claude Code 패널 확인

- 좌측 사이드바에 Claude 아이콘이 보이면 성공
- 클릭 → 입력창에 `안녕` 입력 → Claude 응답 확인

### QUIZ

**문제:** Claude Code로 "경쟁사 분석 보고서를 만들어줘"라고 요청했다. 터미널과 IDE에서 각각 어떤 차이가 있나?

| 선택지 | 내용 |
| ------ | ---- |
| A | 결과는 동일하지만, IDE에서는 생성된 파일을 에디터에서 바로 확인할 수 있다 |
| B | 터미널에서는 파일이 생성되지 않고, IDE에서만 파일이 생성된다 |
| C | IDE에서는 자동으로 Notion에 저장되고, 터미널에서는 로컬에만 저장된다 |

**정답:** A — 터미널이든 IDE든 Claude Code의 기능은 동일하다. 차이는 IDE에서는 생성·수정된 파일이 에디터 탭에 즉시 열려서, 결과를 보면서 피드백할 수 있다는 점이다.

---

## Part 11: 첫 대화

### EXPLAIN

Claude Code가 설치되었다. 이제 실제로 써보자.

**핵심 개념:** Claude Code는 **폴더 단위**로 작업한다. 어떤 폴더를 열었느냐에 따라 Claude의 작업 범위가 결정된다.

**핵심 워크플로우 — 4단계 작업법:**

Part 2에서 배운 권한 모드를 활용한 실전 패턴이다. 처음 익힐 때는 이 순서를 강력 추천한다.

| 단계 | 이름 | 하는 일 | 권한 모드 |
| ---- | ---- | ------- | --------- |
| **1단계** | 🔍 탐색 | 현재 상태 파악 | **Plan mode** (읽기 전용) |
| **2단계** | 📋 계획 | 작업 전략 수립·검토 | Plan mode 유지 |
| **3단계** | 🛠 실행 | 계획대로 파일 생성·수정 | **Ask permissions** 전환 |
| **4단계** | ✅ 정리 | 결과 확인, 피드백 | Ask permissions 유지 |

> **모드 전환:** IDE에서는 Claude Code 패널 입력창의 드롭다운으로 Plan mode ↔ 실행 모드를 전환한다. Plan mode는 읽기 전용이라 안전하고, 잘못된 방향 실행을 예방한다.

**기획자의 첫 요청 예시:**

| 상황 | 프롬프트 |
| ---- | -------- |
| 빈 폴더에서 시작 | "이 프로젝트의 README.md를 만들어줘. 프로젝트명은 '서비스 기획'이야." |
| 기존 파일 분석 | "이 폴더에 있는 파일들을 읽고, 어떤 프로젝트인지 요약해줘." |
| 산출물 생성 | "경쟁사 분석 보고서 템플릿을 마크다운으로 만들어줘." |

**Chat과의 차이를 체감하는 포인트:**

- Claude Chat: "보고서 써줘" → 대화창에 텍스트로 출력
- Claude Code: "보고서 써줘" → **실제 파일이 내 폴더에 생성됨**

### EXECUTE

IDE에서 아래를 따라해보세요:

**Step 1:** 바탕화면에 `my-first-project` 폴더 생성 → IDE에서 열기 (File → Open Folder)

**Step 2 (탐색):** Claude Code 패널을 **Plan mode**로 전환 후 입력:

```text
이 폴더의 현재 상태를 파악해줘.
```

- Plan mode에서는 파일을 읽기만 한다. 빈 폴더라는 것을 확인

**Step 3 (계획):** Plan mode 유지한 채 입력:

```text
이 프로젝트의 README.md와 경쟁사 분석 템플릿(competitor-analysis.md)을 만들 계획을 세워줘.
README: 프로젝트명 "나의 첫 Claude Code 프로젝트" + 한 줄 설명 + 오늘 날짜
경쟁사 분석: 회사명, 주요 기능, 가격, 강점, 약점 비교 테이블 (3곳)
```

- Claude가 제시하는 계획을 검토

**Step 4 (실행):** **Ask permissions** 모드로 전환 후 입력:

```text
계획대로 실행해줘.
```

- Claude가 파일을 생성할 때마다 허락을 요청 → 확인 후 승인
- 에디터에서 생성된 파일을 바로 확인

**Step 5 (정리):** 결과물 확인

- 에디터에서 README.md와 competitor-analysis.md가 생성된 것을 확인
- 내용이 기대와 다르면 피드백: "경쟁사 분석 테이블에 '시장 점유율' 열도 추가해줘"

### QUIZ

**문제:** Claude Code로 복잡한 작업을 시작할 때 권장되는 첫 번째 단계는?

| 선택지 | 내용 |
| ------ | ---- |
| A | Bypass permissions로 전환하고 바로 실행 요청 |
| B | Plan mode로 전환하고 현재 상태를 먼저 탐색 |
| C | 터미널에서 파일 목록을 직접 확인 |

**정답:** B — 4단계 워크플로우의 첫 단계는 Plan mode로 탐색하는 것이다. Plan mode는 읽기 전용이라 안전하며, 현재 상태를 파악한 후에 계획을 세우고 실행하는 것이 잘못된 방향으로 진행하는 것을 예방한다.

---

## Part 12: 다음 단계 — Lv.2로 가는 길

### EXPLAIN

축하합니다! Lv.1을 완료했습니다.

**지금까지 배운 것:**

| Part | 배운 것 | 핵심 한 줄 |
| ---- | ------- | ---------- |
| 1 | AI 용어 | LLM, 토큰, 컨텍스트 윈도우, 프롬프트, 할루시네이션 |
| 2 | 제품 라인업 | Chat/Cowork/Code 판단 + 권한 모드 4단계 |
| 3 | 토큰과 비용 | `/clear`·`/compact` 관리법 + 메모리 시스템 |
| 4 | 보안 | 유료 플랜은 학습 미사용. 민감 데이터는 익명화 |
| 5 | 프롬프트 | 8가지 재료 + ODAF(O-D-A-F) 실전 공식 |
| 6 | 산출물 포맷 | md/csv가 최적. 마크다운 기초 문법 5가지 |
| 7 | 토큰 절약 | 새 세션 + 출력 길이 지정이 가장 효과적 |
| 8 | 터미널 기초 | pwd, ls, cd, cd .., mkdir — 5개면 충분 |
| 9 | CLI 설치 | 한 줄 명령어로 설치 완료 |
| 10 | IDE 세팅 | IDE(VS Code 권장) + Claude Code Extension |
| 11 | 첫 대화 | 4단계 작업법(탐색→계획→실행→정리) 체험 |
| 12 | 다음 단계 | 지금 여기 |

**Lv.1에서 Lv.2로 — 관점의 전환**

Lv.1에서는 "좋은 프롬프트를 매번 잘 쓰는 법"을 배웠다. 하지만 생각해보면, 매번 같은 규칙(한국어, 마크다운, 존댓말, 선택지 포함 등)을 반복하고 있지 않은가? ODAF를 아무리 잘 써도, 프로젝트 규칙은 매 세션마다 다시 입력해야 한다.

**Lv.2의 핵심 질문:** "반복되는 규칙을 파일로 저장해두면, Claude가 알아서 읽어오게 할 수 있지 않을까?"

이것이 바로 **CLAUDE.md**다. 프롬프트를 "매번 잘 쓰는 것"에서 "한 번 쓰고 자동 적용"으로 업그레이드하는 것.

그리고 하나 더 — Lv.1에서 마크다운은 "Claude에게 받는 산출물 포맷"이었다. Lv.2에서는 **마크다운이 "Claude에게 주는 지시 파일"**이 된다. CLAUDE.md, SKILL.md 등 마크다운 파일이 Claude의 행동을 정의한다. 여러분이 이미 잘 하는 "문서 작성"이 곧 Claude 확장 역량이 되는 것이다.

| Lv.1에서 배운 것 | Lv.2에서 확장되는 것 |
| ---------------- | ------------------- |
| ODAF로 매번 좋은 프롬프트 쓰기 | CLAUDE.md로 한 번 쓰면 매 세션 자동 적용 |
| 마크다운으로 산출물 받기 | 마크다운으로 Claude의 행동을 설계하기 |
| 토큰 아끼기 (새 세션 전략) | CLAUDE.md 200줄 이하 유지 (같은 원리) |
| Chat/Cowork/Code 판단 | Code 안에서 Skills/MCP/Subagent 확장 |

Lv.2 시작 방법의 자세한 절차는 **부록 C: Lv.2 시작 가이드**를 참고하자.

**다음 단계: Claude Workshop Lv.2**

Lv.2에서는 Claude Code를 **확장**하는 방법을 배운다:

| Lv.2 세션 | 주제 | 한 줄 설명 |
| --------- | ---- | ---------- |
| Session 1 | CLAUDE.md | 프로젝트 규칙을 한 번 쓰면 매 세션 자동 적용 |
| Session 2 | Skills | 대화 키워드에 따라 전문 지식 자동 로딩 |
| Session 3 | MCP & Hooks | Notion/Slack 연결 + 자동 품질 체크 |
| Session 4 | Subagents | 독립 에이전트에게 작업 위임 |
| Workshop | Plugins | 팀 전체에 워크플로우 배포 |

**Lv.2 시작 방법:**

> **중요: 환경이 바뀝니다.** Lv.1은 Claude 앱에 파일을 첨부해서 진행했지만, Lv.2부터는 **Claude Code 안에서** 진행합니다. Claude Code가 교육 플랫폼이자 학습 도구가 됩니다.

1. `claude-workshop-lv2` 폴더를 받는다 (ZIP이면 압축 해제)
2. IDE에서 `claude-workshop-lv2` 폴더 열기 (File → Open Folder)
3. Claude Code 패널에서 `/session1-claude-md` 입력
4. 튜터가 안내하는 대로 진행 (진행 방식은 Lv.1과 동일: 설명 → 실습 → 퀴즈)

> **사내 도구 연동이 필요하다면:** Lv.2 과정과 별도로, 사내 Confluence/Jira/Works/GHES를 Claude Code에 연결하는 **NAVER 연동 설정 런북**을 실행할 수 있습니다. Lv.2 완료 후 진행을 추천합니다.

### EXECUTE

Lv.1에서 배운 것을 종합하여, 자신만의 Claude Code 활용 계획을 세워보세요:

```text
나는 [직무]로 일하고 있어.
Lv.1에서 배운 내용을 바탕으로, 내 업무에 Claude Code를 활용할 수 있는 시나리오 3가지를 제안해줘.
각 시나리오에 대해:
1. 어떤 업무에 적용하는지
2. Claude Chat 대신 Code를 쓰면 뭐가 다른지
3. Lv.2의 어떤 세션이 도움될지
```

### QUIZ

**문제:** Claude Code 확장 기능 중 "프로젝트 규칙을 한 번 쓰면 매 세션 자동으로 적용"되는 것은?

| 선택지 | 내용 |
| ------ | ---- |
| A | Skills |
| B | Hooks |
| C | CLAUDE.md |

**정답:** C — CLAUDE.md는 프로젝트 루트에 위치한 마크다운 파일로, 매 세션 시작 시 자동으로 읽힌다. Lv.2 Session 1에서 직접 만들어본다.

---

## 부록 A: 마크다운 치트시트

```markdown
# 제목 1
## 제목 2
### 제목 3

**굵게**
*기울임*
~~취소선~~

- 순서 없는 목록
- 항목 2
  - 하위 항목

1. 순서 있는 목록
2. 항목 2

| 열1 | 열2 | 열3 |
| --- | --- | --- |
| 값1 | 값2 | 값3 |

> 인용문

`인라인 코드`

[링크 텍스트](URL)

---  ← 구분선
```

---

## 부록 B: 용어 사전

**영문 용어 (알파벳순):**

| 용어 | 설명 |
| ---- | ---- |
| **CLI** | Command Line Interface. 텍스트로 명령을 내리는 인터페이스 |
| **Extension** | IDE에 추가 기능을 설치하는 플러그인 |
| **IDE** | Integrated Development Environment. 코드 편집기 (VS Code, Antigravity 등) |
| **LLM** | Large Language Model. 대규모 텍스트로 학습한 AI 모델 |
| **MCP** | Model Context Protocol. Claude가 외부 서비스와 소통하는 표준 |
| **Plan mode** | Claude Code의 읽기 전용 모드. 파일을 읽기만 하고 변경하지 않아 탐색·분석에 안전 |

**한글 용어 (가나다순):**

| 용어 | 설명 |
| ---- | ---- |
| **권한 모드** | Claude Code의 작업 허용 범위 설정. Plan mode → Ask permissions → Auto accept → Bypass 순으로 권한이 넓어짐 |
| **마크다운** | `.md` 확장자. 간단한 문법으로 구조화된 문서를 작성하는 포맷 |
| **메모리** | Claude Code가 대화에서 배운 내용을 세션 간 자동 기억하는 시스템. `/memory`로 관리 |
| **컨텍스트 윈도우** | AI가 한 대화에서 기억할 수 있는 총 토큰 용량 |
| **토큰** | AI가 텍스트를 처리하는 최소 단위이자 비용 계산 기준 |
| **프롬프트** | AI에게 보내는 지시·질문 텍스트 |
| **할루시네이션** | AI가 사실처럼 보이는 거짓 정보를 생성하는 현상 |

**슬래시 명령어:**

| 명령어 | 설명 |
| ------ | ---- |
| **`/clear`** | Claude Code 대화를 완전 초기화하는 명령어. 작업 간 전환 시 필수 |
| **`/compact`** | Claude Code 대화를 요약·압축하는 명령어. 맥락을 유지하면서 토큰을 절약 |

---

## 부록 C: Lv.2 시작 가이드

**준비물:**

- `claude-workshop-lv2` 폴더 (ZIP이면 압축 해제)
- IDE(VS Code 권장) + Claude Code Extension 설치 완료

**시작 순서:**

1. IDE에서 `claude-workshop-lv2` 폴더 열기 (File → Open Folder)
2. Claude Code 패널에서 `/session1-claude-md` 입력
3. 튜터의 안내에 따라 Block 0부터 순서대로 진행

**Lv.2 커리큘럼:**

```text
Session 1  →  Session 2  →  Session 3  →  Session 4  →  Workshop
 CLAUDE.md      Skills      MCP & Hooks     Agents       Plugins
  ★☆☆☆☆       ★★★☆☆       ★★★★☆        ★★★★★        실습
  ~40분         ~35분         ~30분          ~35분         ~25분
```
